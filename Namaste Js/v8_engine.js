// ! JIT -> JUST IN TIME COMPILATION
// Interpreter -> line by line || fast
// Compiler  -> code is compiled before execution || efficency ||optimize the code

// aot -> ahead of time compilation

// memory heap and call stack

// ---------------

// JS CODE ->
// PARSING -> AST (abstract syntax tree)
// COMPILATION -> INTERPRETTER (BYTE CODE ) -> COMPILER -> OPTIMIZE CODE -> BYTE CODE
// EXECUTION ->

// garbage collector
// ! mark and sweep algo
// inlining
// copy elision
// inline caching

// -----------------
